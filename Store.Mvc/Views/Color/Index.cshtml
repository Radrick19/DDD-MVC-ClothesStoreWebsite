@using Store.Application.Dto.Administration;
@model IEnumerable<ColorDto>;
@{
    ViewData["Title"] = "Управление цветами";
    Layout = "~/Views/Shared/_DataManagementLayout.cshtml";
}
<div class="manage-top-bar">
    <h1>Цвета</h1>
    <a asp-controller="color" asp-action="add">Создать новый</a>
</div>
<div class="manage-wrapper">
    <table>
        <tr>
            <th>Название</th>
            <th>Hex</th>
            <th>Порядок</th>
        </tr>
        @foreach (var item in @Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Hex</td>
                <td>@item.Order</td>
                <td><a asp-controller="color" asp-action="update" asp-route-id="@item.Id" class="manage-wrapper-edit-button">изменить</a></td>
                <td>
                    <form asp-controller="color" asp-action="delete" asp-route-id="@item.Id"><input type="submit" value="удалить" class="manage-wrapper-delete-button" /></form>
                </td>
            </tr>
        }
    </table>
</div>

